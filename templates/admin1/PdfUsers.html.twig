<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .header {
            text-align: right;
            margin-bottom: 20px;
        }

        .logo {
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        .title {
            text-align: center;
            font-size: 24px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
<div class="header">
    Date et heure actuelles : {{ "now"|date('Y-m-d H:i') }}
</div>

<div class="logo">
    <img src="{{ asset('images/aaa.jpg') }}" style="width: 100px; height: 100px;" alt="Description de votre image">
</div>

<div class="title">
    <h1>LISTES DES Users</h1>
</div>

<div>
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Prenom</th>
            <th>Mail</th>
            <th>Role</th>
            <th>NumTlf</th>
            <th>Ville</th>
            <th>Sexe</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.prenom }}</td>
                <td>{{ user.Email }} TND</td>
                <td>{{ user.role }}</td>
                <td>{{ user.numerotelephone }}</td>
                <td>{{ user.ville }}</td>
                <td>{{ user.sexe }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</body>
</html>
