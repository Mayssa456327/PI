
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sign in and Sign up Form{% endblock %}</title>
    {% block css %}
        <link href="{{ asset('Front/css/user/stylelogin.css') }}" rel="stylesheet" />
        <link href="{{ asset('Front/css/user/login.css') }}" rel="stylesheet" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <style>
            select {
                background-color: transparent;
                border: none;
                outline: none;
                appearance: none;
                width: 100%;
                border: none;
                border-radius: 10px;
                outline: none;
                font-size: 1.8rem;
                font-weight: bold;
                color: #999999;
            }
            .hidden {
                display: none;
            }
            .forget-password-btn {
                text-decoration: none;
                color: #555;
                font-size: 14px;
                margin-top: 10px;
                display: block;
            }

            .forget-password-btn:hover {
                color: #007bff; /* Change the color on hover if desired */
            }
        </style>
    {% endblock %}
</head>
<body>
{% block body %}
    <div class="container">
        <div class="forms-container">
            <div class="signin-signup">
                <form method="POST" action="{{ path('app_login') }}" class="sign-in-form">
                    <!-- Votre formulaire de connexion existant -->
                    <h2 class="title">Sign in</h2>
                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input type="email" placeholder="Email" name="email" required />
                    </div>
                    <div class="input-field">
                        <i class="fas fa-lock"></i>
                        <input type="password" placeholder="Password" name="password" required />
                    </div>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    <input type="hidden" name="login" value="login">
                    <input type="submit" value="Login" class="btn solid" />

                    <!-- Forget Password Button -->
                    <a href="{{ path('forgotten_password') }}" class="forget-password-btn">Forget Password?</a>

                    <p class="social-text">Or Sign in with social platforms</p>
                    <div class="social-media">
                        <a href="#" class="social-icon">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="fab fa-twitter"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="fab fa-google"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                    </div>
                </form>

                <form method="POST" action="{{ path('app_login') }}" class="sign-up-form">
                    <!-- Votre formulaire d'inscription existant -->
                    <h2 class="title">Sign up</h2>
                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input type="text" name="nom" placeholder="Nom" required autocomplete="off" />
                    </div>
                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input type="text" name="prenom" placehohlder="Prenom" required autocomplete="off" />
                    </div>
                    <div class="input-field">
                        <i class="fas fa-envelope"></i>
                        <input type="email" name="email" placeholder="Email" id="email" autocomplete="off" required pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" onblur="checkEmail()" />
                    </div>
                    <i><span id="email-error" style="color: red; font-weight: bold;"></span></i>
                    <div class="input-field">
                        <i class="fas fa-mobile"></i>
                        <input type="tel" name="mob" placeholder="Mobile Number" id="mob" autocomplete="off" onblur="checkPhone()" />
                    </div>
                    <span id="phone-error" style="color: red; font-weight: bold;"></span>
                    <div class="input-field" id="pw">
                        <i class="fas fa-lock" id="icone"></i>
                        <input type="password" name="password" placeholder="Password" id="pass" autocomplete="off"  />
                        <button type="button" id="show-password-togglel">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                    <i><span id="password-error" style="color: #93bfb6; font-weight: bold; font-size:12px; right:1px;"></span></i>
                    <div class="input-field" id="pww">
                        <i class="fas fa-lock"></i>
                        <input type="password" name="confirmpass" placeholder="Confirm password" id="confirmpass" autocomplete="off"  />
                        <button type="button" id="show-cpassword-togglel">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                    <i><span id="cpassword-error" style="color: #93bfb6; font-weight: bold; font-size:12px;"></span></i>
                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <select name="userType" id="userType" required>
                            <option value="" disabled selected>Choisissez le type d'utilisateur</option>
                            <option value="patient">Patient</option>
                            <option value="medecin">Médecin</option>
                        </select>
                    </div>
                    <input type="submit" name="signup" class="btn" value="Sign up" />

                </form>
            </div>
        </div>

        <div class="panels-container">
            <div class="panel left-panel">
                <div class="content">
                    <h3>New here?</h3>
                    <p>Welcome! Sign up to access all features. Already have an account? Enter your information.</p>
                    <a href="{{ path('app_register') }}"><button class="btn transparent">Sign Up</button></a>
                    <a href="{{ path('homePage') }}"><button class="btn transparent">Home</button></a>
                </div>
            </div>
            <div class="panel right-panel">
                <div class="content">
                    <h3>One of us?</h3>
                    <p>Welcome to our  community! Join us by creating an account to access all the features we offer.
                        <button class="btn transparent" id="sign-in-btn">Sign in</button>
                        <a href="{{ path('homePage') }}"><button class="btn transparent">Home</button></a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('Front/js/user/app.js') }}"></script>
    <script src="{{ asset('Front/js/user/control.js') }}"></script>
    <script src="{{ asset('Front/js/user/form.js') }}"></script>
{% endblock %}
</body>
</html>

