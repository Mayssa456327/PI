{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}

    {{ form_widget(form) }}
    
    <button class="bbtn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
