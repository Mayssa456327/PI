{% extends 'base2.html.twig' %}

{% block title %}Top Liked Comments{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-5 mb-4"></h1>
        <div class="row row-cols-1 row-cols-md-3 g-4">
            {% for topLikedComment in topLikedComments %}
                <div class="col">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">{{ topLikedComment[0].getContent() }}</h5>
                            <div class="d-flex justify-content-between align-items-center">
                                <p class="card-text mb-0">Total Comments: {{ topLikedComment['totalComments'] }}💬</p>
                                <p class="card-text mb-0">
                                    Total likes:{% set totalLikes = topLikedComment[0].getLikes() %}
                                    {% for i in 1..totalLikes %}
                                        👍
                                    {% endfor %}
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
{% endblock %}
