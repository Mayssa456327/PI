<form method="post" action="{{ path('app_hazem_delete', {'id': hazem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hazem.id) }}">
    <button class="btn">Delete</button>
</form>
